diff --git a/internal/ate/ixate.go b/internal/ate/ixate.go
index e0b2726..a77571e 100644
--- a/internal/ate/ixate.go
+++ b/internal/ate/ixate.go
@@ -836,7 +836,7 @@ func validateProtocolStart(ctx context.Context, ix *ixATE) ([]string, error) {
 		retryWait      = 10 * time.Second
 		maxRetriesLSPs = 60
 	)
-	maxRetriesProtocols := 5
+	maxRetriesProtocols := 10
 	if len(ix.routeTableToIxFile) > 0 {
 		// Protocols may need extra time to start on large-scale route imports.
 		maxRetriesProtocols = 30
