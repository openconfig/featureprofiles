---
name: Go

on:
  push:
    branches: [ main ]
  pull_request:
  schedule:
    - cron: "0 0 * * *"

jobs:
  common:
    uses: openconfig/common-ci/.github/workflows/go.yml@acb3a87c3f412dda86c3458fa560c19c5e0f8c32
    with:
      coverage-excludes-regex: "/.*test.*"
      tests-excludes-regex: "/.*test.*"
      race-tests-excludes-regex: "/.*test.*"
